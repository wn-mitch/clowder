# Files exempt from scripts/check_step_contracts.sh on a temporary
# basis — resolvers that have not yet been migrated to the
# StepOutcome<W> + 5-heading rustdoc preamble contract
# (CLAUDE.md §"GOAP Step Resolver Contract").
#
# Remove an entry when the corresponding file has been migrated.
# When this file is empty, the allowlist mechanism can be removed
# from the lint script entirely.
#
# §Phase 5a: audit complete — all resolvers migrated. The allowlist
# is now empty. Leaving the file in place so future authors
# migrating new resolvers can temporarily whitelist during WIP.
