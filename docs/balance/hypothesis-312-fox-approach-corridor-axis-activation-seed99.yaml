# Ticket 312 — fox-approach-corridor axis activation, seed-99.
#
# Companion to hypothesis-312-fox-approach-corridor-axis-activation.yaml.
# Three-seed validation per 285 / 297's iteration discipline.
#
# Seed-99's fox patrol topology differs from seed-42's; if the corridor
# axis lifts seed-42 but not seed-99 (or vice versa), the architecture
# is topology-dependent — useful information for the FO-3 first-light
# decision but not by itself a substrate defect.
#
# Iteration policy: see hypothesis-312-fox-approach-corridor-axis-activation.yaml.

hypothesis: |
  On seed-99 the corridor topology may differ from seed-42 — the lift's
  magnitude (and direction) is a function of overlap between fox patrol
  routes and the threat-axis's already-saturated tiles. Preservation or
  modest lift is expected; a drop would suggest the lift is pulling wards
  off topology that this seed's fox patrols traverse on their own.
constants_patch:
  scoring:
    ward_fox_approach_corridor_weight: 0.3
prediction:
  metric: shadow_foxes_avoided_ward_total
  direction: increase
  rough_magnitude_pct: [-15, 100]
seeds: [99]
reps: 1
duration: 900
