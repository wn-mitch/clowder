---
id: 054
title: §7.7.c Fate event-vocabulary expansion — Calling, destiny, fated-pair convergence
status: done
cluster: magic-mythic
initiative: [mythic-texture]
added: 2026-04-27
parked: null
blocked-by: []
supersedes: []
related-systems: [ai-substrate-refactor.md, the-calling.md]
related-balance: []
landed-at: null
landed-on: 2026-05-14
---

> **Retired 2026-05-14 without action.** See the *Retirement closeout*
> section below for the reasoning. The original `## Why` / `## Scope` /
> `## Approach` content is preserved as historical context for whichever
> future subsystem (Calling, destiny, or a small Message-migration
> ticket) reacquires each child's intent.

## Why

Today's `src/systems/fate.rs` emits only `FatedLove` / `FatedRival`.
Aspirations that should respond to the Calling, destiny modifiers, or
fated-pair convergence need those events to exist.

## Scope

- New event variants on the Fate axis: at minimum `CallingStirred`,
  `DestinyMomentum` (or analogous; spec under-specified here),
  `FatedPairConverged`.
- §7.7.c aspiration consumers wired to react to each.
- Cross-link with the Calling subsystem doc as that work matures.

## Gating

The Calling subsystem itself is documented at
`docs/systems/the-calling.md` but has no implementation yet and no
current open-work ticket. Any worker picking this up should first
scope a Calling subsystem ticket (or confirm the design is adequate
to ship `CallingStirred` as an event without the full subsystem) —
that's a precondition decision, not a `blocked-by`.

The `the-calling.md` doc is rank 3 in
`docs/systems-backlog-ranking.md` (cross-cutting debt; lands alongside
Calling implementation).

## Out of scope

- The full Calling subsystem implementation. This ticket commits the
  *event vocabulary* shape; the subsystem that *fires* most of these
  events is its own scope.

## Approach

Two routes. Route A: open a Calling subsystem ticket first, gate this
on it. Route B: ship the event variants empty-bodied so consumers can
be wired now and the Calling system fills them in later. Route A is
cleaner; Route B is faster but accumulates ghost events.

## Verification

- Unit tests on each new event variant.
- Integration test: §7.7.c aspiration consumer fires when its target
  event fires.
- `just check` green.
- Soak verdict: depends on which events are wired and whether the
  Calling subsystem fires any in the soak window.

## Related work

<!-- linkages:start -->
<!-- generated by `just similar-link-report` on 2026-05-08 — review and prune; pairs above threshold that aren't already cross-referenced. -->

- · ** 35** (ready, —, score 0.81) — Burial — implement the §5 broaden-sideways capability so the continuity canary…
- · ** 53** (blocked, —, score 0.81) — §7.7.b death-event grief emission — relationship-classified survivors payload
- ✓ landed ** 14** (done, —, score 0.80) — "§4.2 State marker trio — `InCombat` / `OnCorruptedTile` / `OnSpecialTerrain` a…

<!-- linkages:end -->
## Log

- 2026-04-27: opened from ticket 013 retirement (spec-follow-on debts
  umbrella decomposition). Original sub-task 13.3 in spec
  `docs/systems/ai-substrate-refactor.md` §7.7.c.
- 2026-05-14: retired without action. See *Retirement closeout* below.

## Retirement closeout (2026-05-14)

**Why retired.** Each of the three proposed event variants has a
natural home elsewhere or no longer fits the work surface:

- `CallingStirred` — belongs in the Calling subsystem ticket whenever
  that subsystem gets scoped. Per CLAUDE.md substrate discipline
  ("substrate stubs are forbidden"), the producer (Calling state
  machine) and consumer (aspirations reacting to it) should land
  together; pre-shipping the event vocabulary as a ghost type is a
  substrate stub. The Calling is rank 3 in
  `docs/systems-backlog-ranking.md` (Worthwhile, cross-cutting
  mythic-texture pillar) and has no current scoping ticket.
- `DestinyMomentum` — spec under-specified at open-time and no design
  surface has emerged since. Defer until a "destiny modifier" concept
  has a coherent home in either `the-calling.md` or a follow-on
  axis-capture (§7.W) ticket.
- `FatedPairConverged` — semantically equivalent to the existing
  `awaken_fated_connections` transition
  (`src/systems/fate.rs:220-325`), which already records
  `Feature::FateAwakened` when fated pairs come into proximity. The
  only gap is exposing this transition as a witnessable `Message` per
  ticket 258's convention. Deferred until a consumer ticket actually
  needs the Message shape — the current readers (`FatedLoveLift` /
  `FatedRivalLift` in `src/ai/modifier.rs`) read the component
  directly. When a future redesign (narrative-emission rework, or an
  aspiration milestone that redirects on convergence) wants to consume
  it, that consumer opens the Message migration as its own prereq via
  `blocked-by`.

**Post-320/321 framing.** The aspiration consumer pipeline
(`Milestone.emits[]` + L1→L2 picker in
`src/systems/aspiration_picker.rs` + HTN method registry) is now the
canonical home for any new event vocabulary that aspirations should
react to. New event types that don't ship with paired Live methods or
substrate-discipline-compliant dormant methods + glue tickets
accumulate as unconsumed surface area. The three proposed variants
fail that test for different reasons (no producer / no design / no
consumer-yet); retiring the umbrella surfaces those gaps as the
responsibility of whichever subsystem ticket eventually reacquires
each child.

**Successor pointers.** No successor tickets opened automatically;
each child reacquires a home when its parent subsystem (Calling /
destiny / Message-migration) gets scoped. The §7.7.c section of
`docs/systems/ai-substrate-refactor.md` (lines 4727-4742) is left
intact — it's still an accurate description of the current state and
the right place for a future worker to start re-deriving the debt.

**Epic dashboard updated.** `060-ai-substrate-refactor-epic.md` no
longer references 054 in its Phase 6b row / dedicated row / §7.7
status line / parenthetical aspiration list.
